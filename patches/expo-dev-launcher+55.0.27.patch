diff --git a/node_modules/expo-dev-launcher/expo-dev-launcher.podspec b/node_modules/expo-dev-launcher/expo-dev-launcher.podspec
index 6c05a4d..1b8a7ce 100644
--- a/node_modules/expo-dev-launcher/expo-dev-launcher.podspec
+++ b/node_modules/expo-dev-launcher/expo-dev-launcher.podspec
@@ -9,6 +9,7 @@ rescue
 end
 
 reactNativeTargetVersion = reactNativeVersion.split('.')[1].to_i
+has_react_app_dependency_provider = !Dir.glob(File.join(File.dirname(`node --print "require.resolve('react-native/package.json')"`), '**', 'ReactAppDependencyProvider.podspec{,.json}')).empty?
 
 Pod::Spec.new do |s|
   s.name           = 'expo-dev-launcher'
@@ -108,7 +109,9 @@ Pod::Spec.new do |s|
   s.dependency "EXUpdatesInterface"
   s.dependency "expo-dev-menu"
   s.dependency "ExpoModulesCore"
-  s.dependency "ReactAppDependencyProvider"
+  if has_react_app_dependency_provider
+    s.dependency "ReactAppDependencyProvider"
+  end
  add_dependency(s, "React-jsinspector", :framework_name => 'jsinspector_modern')
 
  unless defined?(install_modules_dependencies)
diff --git a/node_modules/expo-dev-launcher/ios/EXDevLauncherController.h b/node_modules/expo-dev-launcher/ios/EXDevLauncherController.h
--- a/node_modules/expo-dev-launcher/ios/EXDevLauncherController.h
+++ b/node_modules/expo-dev-launcher/ios/EXDevLauncherController.h
@@ -39,7 +39,7 @@
 
 @end
 
-@interface EXDevLauncherController : RCTDefaultReactNativeFactoryDelegate <RCTBridgeDelegate, EXUpdatesExternalInterfaceDelegate>
+@interface EXDevLauncherController : NSObject <RCTBridgeDelegate, EXUpdatesExternalInterfaceDelegate>
 
 @property (nonatomic, weak) EXAppContext * _Nullable appContext;
 @property (nonatomic, strong) EXDevLauncherPendingDeepLinkRegistry *pendingDeepLinkRegistry;
diff --git a/node_modules/expo-dev-launcher/ios/EXDevLauncherController.m b/node_modules/expo-dev-launcher/ios/EXDevLauncherController.m
--- a/node_modules/expo-dev-launcher/ios/EXDevLauncherController.m
+++ b/node_modules/expo-dev-launcher/ios/EXDevLauncherController.m
@@ -15,9 +15,6 @@
 #import <EXDevLauncher/EXDevLauncherUpdatesHelper.h>
 #import <EXDevLauncher/RCTPackagerConnection+EXDevLauncherPackagerConnectionInterceptor.h>
 
-
-#import <ReactAppDependencyProvider/RCTAppDependencyProvider.h>
-
 #if __has_include(<EXDevLauncher/EXDevLauncher-Swift.h>)
 // For cocoapods framework, the generated swift header will be inside EXDevLauncher module
 #import <EXDevLauncher/EXDevLauncher-Swift.h>
@@ -81,8 +78,6 @@ static const NSTimeInterval EXDevLauncherDefaultRequestTimeout = 10.0;
     self.errorManager = [[EXDevLauncherErrorManager alloc] initWithController:self];
     self.installationIDHelper = [EXDevLauncherInstallationIDHelper new];
     self.shouldPreferUpdatesInterfaceSourceUrl = NO;
-
-    self.dependencyProvider = [RCTAppDependencyProvider new];
     self.devMenuDelegate = [[DevLauncherDevMenuDelegate alloc] initWithController:self];
     [[DevMenuManager shared] setDelegate:self.devMenuDelegate];
   }
diff --git a/node_modules/expo-dev-launcher/ios/ReactDelegateHandler/ExpoDevLauncherReactDelegateHandler.swift b/node_modules/expo-dev-launcher/ios/ReactDelegateHandler/ExpoDevLauncherReactDelegateHandler.swift
--- a/node_modules/expo-dev-launcher/ios/ReactDelegateHandler/ExpoDevLauncherReactDelegateHandler.swift
+++ b/node_modules/expo-dev-launcher/ios/ReactDelegateHandler/ExpoDevLauncherReactDelegateHandler.swift
@@ -31,7 +31,7 @@ private class DevLauncherWrapperView: UIView {
 
 @objc
 public class ExpoDevLauncherReactDelegateHandler: ExpoReactDelegateHandler, EXDevLauncherControllerDelegate {
-  private weak var reactNativeFactory: RCTReactNativeFactory?
+  private weak var reactNativeFactory: AnyObject?
   private weak var reactDelegate: ExpoReactDelegate?
   private var launchOptions: [AnyHashable: Any]?
   private var rootViewModuleName: String?
@@ -74,12 +74,12 @@ public class ExpoDevLauncherReactDelegateHandler: ExpoReactDelegateHandler, EXDe
 
   @objc
   public func isReactInstanceValid() -> Bool {
-    return self.reactNativeFactory?.rootViewFactory.value(forKey: "reactHost") != nil
+    return rootViewFactory()?.value(forKey: "reactHost") != nil
   }
 
   @objc
   public func destroyReactInstance() {
-    self.reactNativeFactory?.rootViewFactory.setValue(nil, forKey: "reactHost")
+    rootViewFactory()?.setValue(nil, forKey: "reactHost")
   }
 
   // MARK: EXDevelopmentClientControllerDelegate implementations
@@ -90,22 +90,15 @@ public class ExpoDevLauncherReactDelegateHandler: ExpoReactDelegateHandler, EXDe
       fatalError("`reactDelegate` should not be nil")
     }
 
-    self.reactNativeFactory = reactDelegate.reactNativeFactory as? RCTReactNativeFactory
+    self.reactNativeFactory = reactDelegate.reactNativeFactory as AnyObject
 
     // Reset rctAppDelegate so we can relaunch the app
     if RCTIsNewArchEnabled() {
-      self.reactNativeFactory?.rootViewFactory.setValue(nil, forKey: "_reactHost")
+      rootViewFactory()?.setValue(nil, forKey: "_reactHost")
     } else {
-      self.reactNativeFactory?.bridge = nil
-      self.reactNativeFactory?.rootViewFactory.bridge = nil
+      reactNativeFactoryObject()?.setValue(nil, forKey: "bridge")
+      rootViewFactory()?.setValue(nil, forKey: "bridge")
     }
-
-    #if RCT_DEV_MENU
-    // Set core dev menu configuration to disable shortcuts and shake gesture
-    self.reactNativeFactory?.devMenuConfiguration = RCTDevMenuConfiguration(
-      devMenuEnabled: true,
-      shakeGestureEnabled: false,
-      keyboardShortcutsEnabled: false
-    )
-    #endif
 
     let rootView = reactDelegate.reactNativeFactory.recreateRootView(
       withBundleURL: developmentClientController.sourceUrl(),
@@ -164,4 +157,12 @@ public class ExpoDevLauncherReactDelegateHandler: ExpoReactDelegateHandler, EXDe
     // several React instances over a single app lifetime and we want them all to have the same
     // initial properties
   }
+
+  private func reactNativeFactoryObject() -> NSObject? {
+    return self.reactNativeFactory as? NSObject
+  }
+
+  private func rootViewFactory() -> NSObject? {
+    return reactNativeFactoryObject()?.value(forKey: "rootViewFactory") as? NSObject
+  }
 }
diff --git a/node_modules/expo-dev-launcher/android/build.gradle b/node_modules/expo-dev-launcher/android/build.gradle
--- a/node_modules/expo-dev-launcher/android/build.gradle
+++ b/node_modules/expo-dev-launcher/android/build.gradle
@@ -1,18 +1,24 @@
 buildscript {
+  def usesKotlinComposePlugin = !kotlinVersion.toString().startsWith("1.")
   repositories {
     mavenCentral()
   }
   dependencies {
-    classpath("org.jetbrains.kotlin.plugin.compose:org.jetbrains.kotlin.plugin.compose.gradle.plugin:${kotlinVersion}")
+    if (usesKotlinComposePlugin) {
+      classpath("org.jetbrains.kotlin.plugin.compose:org.jetbrains.kotlin.plugin.compose.gradle.plugin:${kotlinVersion}")
+    }
     classpath("org.jetbrains.kotlin.plugin.serialization:org.jetbrains.kotlin.plugin.serialization.gradle.plugin:${kotlinVersion}")
     classpath("com.apollographql.apollo:apollo-gradle-plugin:4.3.1")
   }
 }
 
 apply plugin: 'com.android.library'
 apply plugin: 'expo-module-gradle-plugin'
-apply plugin: 'org.jetbrains.kotlin.plugin.compose'
+if (!kotlinVersion.toString().startsWith("1.")) {
+  apply plugin: 'org.jetbrains.kotlin.plugin.compose'
+}
 apply plugin: 'org.jetbrains.kotlin.plugin.serialization'
 apply plugin: 'com.apollographql.apollo'
@@ -64,9 +70,16 @@ android {
 
   buildFeatures {
+    compose true
     buildConfig true
     viewBinding true
   }
+
+  if (kotlinVersion.toString().startsWith("1.")) {
+    composeOptions {
+      kotlinCompilerExtensionVersion = "1.5.13"
+    }
+  }
 }
